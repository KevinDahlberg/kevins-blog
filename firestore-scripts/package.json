{
  "name": "migrations",
  "version": "0.8.0",
  "description": "database migrations for mpp",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "./node_modules/.bin/tsc -p tsconfig.json",
    "run:migrations": "ts-node src/index.ts run-migrations",
    "secrets:get": "gsutil -m cp 'gs://my-powerpak-firebase-keys/secrets'"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/inquirer": "^7.3.1",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.3"
  },
  "dependencies": {
    "commander": "^7.1.0",
    "firebase-admin": "^9.5.0",
    "inquirer": "^8.0.0"
  }
}
